<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Members Only</title>
  </head>
  <body>
    <%- include('partials/header') %>

    <h2>Welcome to Members Only!</h2>

    <p>
      <% if (!currentUser) { %>
      <a href="/login">Log in</a> or <a href="/signup">Sign up</a> to post
      messages. <% } else { %>
      <a href="/messages/new">Post a new message</a>
      <% } %>
    </p>
  </body>
</html>
